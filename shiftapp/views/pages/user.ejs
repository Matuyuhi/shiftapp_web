<% layout('../layout') -%>

<div class="page-header min-vh-75">
  <div class="container-fulid py-2 px-2">
    <div class="row">
      
      <!-- <div class="col-lg-8 mb-lg-0 mb-4">
        <div class="card z-index-2 px-5">
          <div class="card-header pb-0">
            <h6>Sales overview</h6>
            <p class="text-sm">
              <i class="fa fa-arrow-up text-success"></i>
              <span class="font-weight-bold">4% more</span> in 2021
            </p>
          </div>
          <div class="card-body p-3">
            <div class="chart">
              <canvas id="chart-line" class="chart-canvas" height="250" dat=<%= shifts[0].workcount %>></canvas>
            </div>
          </div>
        </div>
      </div>
      <div class="col-3">
        <div class="card" style="width: 250px;height: 250px;">
          <div class="card-body ">
            <div class="box blue">
              <div class="percent">
                <svg style="width: 200px;height: 200px;transform: rotate(-90deg);">
                  <defs>
                    <linearGradient id="sample">
                      <stop offset="0%" stop-color="orange" />
                      <stop offset="100%" stop-color="red" />
                    </linearGradient>
                  </defs>
                  <circle class="base" cx="100" cy="100" r="70"></circle>
                  <circle id="circle-percent" class="line" cx="100" cy="100" r="70" style="stroke-dashoffset: <%= Number(shifts[0].workcount / 6 * -440)%>;"></circle>
                </svg>
                <p class="text">今週</p>
                <div class="number" style="text-align: center;">
                  <p class="title" style="text-align: center;"><%= shifts[0].workcount %>
                  <div class="unit">日</div>
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div> -->
    </div>
  </div>
</div>
<!-- <script src="/assets/js/plugins/chartjs.min.js"></script>
<script>
  var ele = document.getElementById("chart-line")
  var ctx2 = ele.getContext("2d");
  var gradientStroke1 = ctx2.createLinearGradient(0, 230, 0, 50);

  gradientStroke1.addColorStop(1, 'rgba(203,12,159,0.2)');
  gradientStroke1.addColorStop(0.2, 'rgba(72,72,176,0.0)');
  gradientStroke1.addColorStop(0, 'rgba(203,12,159,0)'); //purple colors

  var gradientStroke2 = ctx2.createLinearGradient(0, 230, 0, 50);

  gradientStroke2.addColorStop(1, 'rgba(20,23,39,0.2)');
  gradientStroke2.addColorStop(0.2, 'rgba(72,72,176,0.0)');
  gradientStroke2.addColorStop(0, 'rgba(20,23,39,0)'); //purple colors
  chart = {}
  chart.label = []
  chart.data = []
  <% for (let num = shifts.length-1; num >= 0 ; num--){ %>
  chart.label.push("<%= String(shifts[num].startday) %>")
  chart.data.push(<%= Number(shifts[num].shift.length) %>)
  <% } %>
  console.log(chart)
  let myChart = new Chart(ctx2, {
    type: "line",
    data: {
      labels: chart.label,
      datasets: [{
        label: "出勤日数",
        tension: 0.4,
        borderWidth: 0,
        pointRadius: 0,
        borderColor: "#cb0c9f",
        borderWidth: 3,
        backgroundColor: gradientStroke1,
        fill: true,
        data: chart.data,
      }, ],
    },
    options: {
      responsive: true,
      maintainAspectRatio: false,
      plugins: {
        legend: {
          display: false,
        }
      },
      interaction: {
        intersect: false,
        mode: 'index',
      },
      scales: {
        y: {
          grid: {
            drawBorder: false,
            display: true,
            drawOnChartArea: true,
            drawTicks: false,
            borderDash: [5, 5]
          },
          ticks: {
            suggestedMax: 7,
            suggestedMin: 0,
            stepSize: 2,
            padding: 0,
            color: '#b2b9bf',
            font: {
              size: 11,
              family: "Open Sans",
              style: 'normal',
              lineHeight: 2
            },
          },

        },
        x: {
          grid: {
            drawBorder: false,
            display: false,
            drawOnChartArea: false,
            drawTicks: false,
            borderDash: [5, 5]
          },
          ticks: {
            display: true,
            color: '#b2b9bf',
            padding: 20,
            font: {
              size: 11,
              family: "Open Sans",
              style: 'normal',
              lineHeight: 2
            },
          }
        },
      },

    },
  });
</script> -->
<% var footer_scripts = (function () {/*
// <script src="/assets/js/plugins/chartjs.min.js"></script>
// <script src="/javascripts/profile.js"></script>
*/}).toString().match(/[^]*\/\*([^]*)\*\/\}$/)[1]; %>
<% block("footer_scripts", footer_scripts) -%>
